#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @d: integer to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int d)
{
	return (d >= '0' && d <= '9');
}
