#include <stdio.h>
/**
 *main - Entry point
 *Description: 'Print alphabets lowercase'
 *Return: always 0
 */
int main(void)
{
	int k = 0;

	for (k = 0; k < 10; k++)
	{
		printf("%d", k);
	}
	return (0);
}
